# Pinata IPFS Configuration
# Get these from: https://app.pinata.cloud/keys

# Option 1: JWT Token (Recommended)
# Create a new API key at https://app.pinata.cloud/keys and copy the JWT token
VITE_PINATA_JWT=your_pinata_jwt_token_here

# Your Pinata Gateway URL (optional)
# You can use the default gateway or your dedicated gateway
VITE_PINATA_GATEWAY=https://gateway.pinata.cloud

# Option 2: API Key + Secret (Alternative)
# If you prefer using API Key and Secret instead of JWT
REACT_APP_PINATA_API_KEY=your_pinata_api_key_here
REACT_APP_PINATA_SECRET_KEY=your_pinata_secret_key_here

# Smart Contract Configuration
# The deployed contract address will be automatically imported from Cert.json
# Make sure to update Cert.json with the correct contract address after deployment
